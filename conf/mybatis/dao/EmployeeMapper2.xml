<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.dao.EmployeeMapper2">
    <!--
        自定义某个javaBean的封装规则
        Type: 自定义规则的java类型
        id：唯一的ID引用
     -->
    <resultMap id="MyEmp" type="mybatis.bean.Employee">
        <!--
            指定主键列的封装规则
            id定义主键会底层进行优化
            column:数据表的类名
            property:JavaBean的字段名
        -->
        <id column="id" property="id"/>
        <!-- 定义普通列的封装规则，不指定的列可以自动封装。推荐把所有的列映射规则都写上 -->
        <result column="last_Name" property="lastName"/>
        <result column="gender" property="gender"/>
        <result column="email" property="email"/>
    </resultMap>

    <select id="getEmpById" resultMap="MyEmp">
        select * from tbl_employee where id = #{id}
    </select>

    <!--
        eg1:
            查询emp的同时查出相应的department
     -->

    <update id="addEmpDeptId" parameterType="boolean">
        update tbl_employee set dept_id = ${deptId} where id = #{id}
    </update>
</mapper>